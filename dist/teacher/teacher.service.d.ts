import { Model } from 'mongoose';
import { Teacher } from './schemas/teacher.schema';
import { CreateTeacherDto } from './dto/create-teacher.dto';
import { UpdateTeacherDto } from './dto/update-teacher.dto';
import { TeacherResponseDto } from './dto/teacher-response.dto';
import { TeacherAssignmentRequestDto } from './dto/teacher-assignment.dto';
import { SchoolService } from '../school/school.service';
import { ClassService } from '../class/class.service';
import { UserService } from '../user/user.service';
import { PaginatedTeachersResponseDto } from './dto/paginated-teachers.dto';
import { ClassDetail } from 'src/class/dto/class-detail.dto';
export declare class TeacherService {
    private teacherModel;
    private schoolService;
    private classService;
    private userService;
    constructor(teacherModel: Model<Teacher>, schoolService: SchoolService, classService: ClassService, userService: UserService);
    private mapToResponseDto;
    create(createTeacherDto: CreateTeacherDto): Promise<TeacherResponseDto>;
    findAll(): Promise<TeacherResponseDto[]>;
    findById(id: string): Promise<TeacherResponseDto>;
    findByMatricule(matricule: string): Promise<TeacherResponseDto>;
    findByUserId(userId: string): Promise<TeacherResponseDto>;
    update(id: string, updateTeacherDto: UpdateTeacherDto): Promise<TeacherResponseDto>;
    remove(id: string): Promise<void>;
    assignToClass(assignmentDto: TeacherAssignmentRequestDto): Promise<TeacherResponseDto>;
    updateSubjects(teacherId: string, subjects: string[]): Promise<TeacherResponseDto>;
    getTeachersBySchool(schoolId: string, page?: number, limit?: number): Promise<PaginatedTeachersResponseDto>;
    getTeacherProfile(userId: string): Promise<TeacherResponseDto>;
    getEducatorsByUserIds(userIds: string[]): Promise<TeacherResponseDto[]>;
    assignTeacherToClass(data: TeacherAssignmentRequestDto): Promise<ClassDetail[]>;
}
